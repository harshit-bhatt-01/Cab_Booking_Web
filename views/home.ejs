<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Oswald:wght@700&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background-color: #0d1531;
      }
      .slider {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh;
        background: #122149;
        z-index: -1;
      }

      .brand {
        width: 200px;
        align-items: center;
        display: flex;
        flex-direction: column;
      }
      .brand-image {
        width: 100px;
        height: 100px;
      }

      .menu {
        display: flex;
        justify-content: space-between;
        width: 200px;
        margin-top: -20px;
      }

      .menu-item {
        display: block;
        height: inherit;
        top: 50%;
        position: relative;
        text-decoration: none;
        color: white;
      }
      /* .menu-item span:hover{
    border-bottom: 1px solid white;
} */
      .brand-image img {
        border-radius: 50%;
      }
      .brand-name {
        /* bottom: -25%; */
        /* position: absolute; */
        font-family: sans-serif;
        font-size: 20px;
        font-weight: 550;
      }

      .nav {
        color: white;
        position: absolute;
        z-index: 3;
        width: 100%;
        display: flex;
        justify-content: space-between;
        padding: 40px;
      }

      #logo {
        grid-column: 2/3;
        font-size: 24px;
      }

      .hamburger {
        justify-self: end;
        width: 30px;
        height: 30px;
      }

      section {
        display: flex;
        height: 100vh;
        justify-content: center;
        align-items: center;
      }

      .hero {
        height: 100%;
        width: 100%;
        position: relative;
        top: 0;
      }

      .hero img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .headline {
        position: absolute;
        top: 70%;
        left: 10%;
        font-size: 60px;
        transform: translate(-20%, -70%);
        color: white;
        z-index: 3;
      }
      .headline2 {
        position: absolute;
        top: 70%;
        left: 18%;
        font-size: 40px;
        transform: translate(-20%, -70%);
        color: white;
        z-index: 3;
        margin-top: 50px;
      }

      .input-form {
        position: absolute;
        top: 80%;
        left: 20%;
        font-size: 45px;
        transform: translate(-20%, -70%);
        color: white;
        z-index: 3;
      }

      input {
        width: 250px;
        padding: 10px;
        outline: none;
        border-radius: 10px;
        font-size: 20px;
      }
      button {
        padding: 7px;
        border: 0.5px solid blue;
        border-radius: 10px;
        background-color: transparent;
        text-decoration: none;
        color: white;
        font-size: 20px;
        cursor: pointer;
      }

      .hero::after {
        content: "";
        background: black;
        width: 100%;
        height: 100%;
        position: absolute;
        left: 0;
        top: 0;
        opacity: 0.35;
      }

      .slider {
        position: absolute;
        display: block;
        left: 0;
        top: 50%;
        margin: 0 auto;
        height: 2px;
        background-color: #fff;
        width: 0%;
        transition: width 0.6s;
      }
      .nav-name {
        font-size: 20px;
        font-weight: 550;
        padding: 10px;
        cursor: pointer;
      }

    </style>
  </head>
  <body>
    <header>
      <section>
        <div class="hero">
          <div class="nav">
            <div class="brand">
              <div class="brand-image">
                <img
                  src="https://play-lh.googleusercontent.com/y2w2QDagpoKkcMnaudzJ6VxmS9dDKovq1DDjONhtcqmDiPcnhWIKEAsEuV8BiaFoOeI"
                  alt=""
                />
              </div>
            </div>
            <div class="menu">
              <a class="menu-item" href="/auth/google"
                ><span class="nav-name"
                  >Login
                  <div class="slider"></div> </span
              ></a>
              <a class="menu-item" href="/auth/google"
                ><span class="nav-name"
                  >Signup
                  <div class="slider"></div> </span
              ></a>

            </div>
          </div>
          <img
            src="https://singularityhub.com/wp-content/uploads/2018/06/autonomous-vehicles-cars-blurred-hong-kong-city-street-view-night-558632776.jpg"
            alt=""
          />
          <h1 class="headline">Booking Taxi Made Easy</h1>
          <h3 class="headline2">Login to continue...</h2>
        </div>
      </section>
    </header>

    <div class="slider"></div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/2.1.3/TweenMax.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/2.1.3/TimelineMax.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.4.0.min.js"></script>
    <script>
      const hero = document.querySelector(".hero");
      const slider = document.querySelector(".slider");
      const logo = document.querySelector(".brand-image");
      const headline = document.querySelector(".headline");
      const headline2 = document.querySelector(".headline2");
      const nav = document.querySelector(".nav");
      const formInput = document.querySelector(".input-form");
      console.log(formInput);
      const tl = new TimelineMax();

      tl.fromTo(hero, 1, { height: "0%" }, { height: "100%", ease: Power2.easeInOut })
        .fromTo(hero, 1.2, { width: "70%" }, { width: "100%", ease: Power2.easeInOut })
        .fromTo(slider, 1.2, { x: "-100%" }, { x: "0%", ease: Power2.easeInOut }, "-=1.2")
        .fromTo(logo, 0.5, { opacity: 0, x: 30 }, { opacity: 1, x: 0 }, "-=0.5")
        .fromTo(
          headline,
          0.5,
          { opacity: 0, x: 30, fontSize: 20 },
          { opacity: 1, x: 0, fontSize: 50, autoRound: false },
          "-=0.5"
        ).fromTo(
          headline2,
          0.5,
          { opacity: 0, x: 30, fontSize: 20 },
          { opacity: 1, x: 0, fontSize: 35, autoRound: false },
          "-=0.5"
        )
        .fromTo(formInput, 0.5, { opacity: 0 }, { opacity: 1 })
        .fromTo(nav, 0.5, { opacity: 0, x: 30 }, { opacity: 1, x: 0 }, "-=0.5")
        
    </script>
    <script>
      $(".name").hover(
        function () {
          $(this).children(".slider").css("width", "100%");
        },
        function () {
          $(this).children(".slider").css("width", "0%");
        }
      );
    </script>
    <script>
      function getLocation() {
        if (navigator.geolocation) {
          navigator.geolocation.getCurrentPosition(showPosition);
        } else {
          x.innerHTML = "Geolocation is not supported by this browser.";
        }
      }
      function showPosition(position) {
        if (position) {
          document.getElementById("lat").value = position.coords.latitude;
          document.getElementById("long").value = position.coords.longitude;
        } else {
          alert("Location required to search cabs");
        }
        document.getElementById("destination_form").submit();
      }
    </script>
  </body>
</html>
